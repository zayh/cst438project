DROP TABLE song;
CREATE TABLE IF NOT EXISTS song (
	song_id BIGINT NOT NULL AUTO_INCREMENT UNIQUE PRIMARY KEY, 
	song_name VARCHAR (100) NOT NULL,
	is_solo_release TINYINT(1) NOT NULL DEFAULT 0,
	band_id BIGINT,
	FOREIGN KEY (band_id) 
		REFERENCES band(band_id) 
		ON DELETE CASCADE
);